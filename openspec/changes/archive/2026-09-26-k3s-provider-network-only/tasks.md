## Implementation Tasks

- [x] Replace create-dialog tenant/provider switching and `Default` auto-selection with an optional external-provider-only Field/SelectInput; explain direct attachment and later internal NICs. Clear a selected provider ID if a new network catalog no longer contains it.
- [x] Confirm existing caller request conversion preserves omitted `network_id` for blank and sends selected external ID, and update only meaningful existing tests that pin obsolete network choices.
- [x] Update `docs/api/k3s.md` with provider-only create UI and post-create internal NIC workflow.
- [x] Smoke the actual local container UI with the real private/external catalog; capture blank and selected create request bodies without creating resources. Verify containment and reachable submission at 390x720, 768x900, 1024x768, and 1440x900. Bound the modal height and allow internal scrolling for short viewports. Afterglow test:gate and frontend amd64/arm64 builds pass.
- [x] Publish and deploy the provider-only frontend to the authorized Kolla production target and verify authenticated UI. Commit `0a40c82c`, CI run `36232647814`, frontend digest `sha256:c603fe1984ad608abd2a9e0d2bb9c74e2df7bafd5584ea8780520e56492ae694`; all three controllers healthy and production options contain only external providers.
- [x] Verify the deployed Drover admission and real two-node provider cluster lifecycle: downloaded kubeconfig, internal NIC hotplug, reboot, unchanged provider pin and Kubernetes readiness, then exact resource cleanup. Passed on 2026-09-26 with cluster `1fa8338a-17d9-4e8f-a717-2cb028fb2077`; see the final lifecycle evidence below.
- [x] Archive after the real cluster lifecycle and final cleanup evidence are complete.
- [x] Diagnose hotplug regression on a temporary production guest: udev `%k` supplied pre-rename `eth0`, creating an unused netplan while actual `ens8` used pbrutil's fallback default route. The `SYSTEMD_WANTS` alternative passed static substitution but did not activate on real hotplug; discard it. Using final `$name` in deferred `RUN` with `systemctl --no-block` passed actual detach/attach and reboot on a retained diagnostic node: only provider `ens3` default route, unchanged node IP, K3s Ready, secondary DNS empty and IPv6 RA disabled. Delete the diagnostic and restore exact resource IDs afterward.
- [x] Complete secondary DHCP verification on a healthy tenant uplink. `dms-compute3` has `br-tenant` physical port `eno1` in `NO-CARRIER`/`DOWN`, so tenant-VLAN DHCP cannot leave that host. Application DHCP settings and shared host networking were not changed to hide it. Only the owned temporary verification VMs were cold-placed on hosts with a carrier-up tenant uplink (`dms-compute2`, `dms-compute4`) before first internal NIC attachment; first attachment and reboot then passed.

## Verification Evidence

- Final Drover source: `849b872736f7c034f64e0d940c31f3e2ac47b153`; [CI 36234670483](https://github.com/openstack-afterglow/drover/actions/runs/36234670483) passed service/SDK checks, MariaDB API-worker smoke, packaging and image publication. Local service suite: 664 passed, 3 skipped; Ruff and staged architecture guard passed.
- Production API: `ghcr.io/openstack-afterglow/drover-api@sha256:775e5fcdc2e57a2f4946e3737e46007efd2f9b6806fb6039dd5e6c2a56abebba`.
- Production worker: `ghcr.io/openstack-afterglow/drover-worker@sha256:6f63f64d25fd6410ffacb702069ecd29a1b91a522f690823475aa4ffe8fc70ee`.
- Canonical local Dockerfile builds cover API, worker and clean-release frontend on `linux/amd64` and `linux/arm64`. Runtime imports and Node execution match both architectures. Canonical development Compose recreated only affected services; canonical Kolla pull/reconfigure deployed digest-pinned images without changing other service image references or deleting volumes.
- Three production controllers run the expected API/worker and healthy frontend/API containers. `https://drover.dmslab.re.kr/v1/health/ready` returns 200 with database, Redis, migrations and Keystone all `ok`; public DNS and verified TLS work.
- Afterglow `npm run test:gate` and staged architecture guard passed; [frontend CI 36232647814](https://github.com/openstack-afterglow/openstack-afterglow/actions/runs/36232647814) passed. Actual authenticated production UI was checked at 390, 768 and 1024px; local UI additionally covered 1440px and captured blank/explicit network request bodies without creating resources.
- Local admission creation could not be exercised end-to-end because its isolated Drover database has no image/flavor/network policies; those settings were not silently copied from production. Production admission and lifecycle verification use the configured administrator `public_provider` default. Live guest testing is Ubuntu; FCOS is covered by generated-userdata/service tests, not an FCOS live cluster.
- Secondary DHCP diagnosis: the guest sends valid DHCP Discover packets through its tap, hybrid bridge and OVS port; both configured Neutron DHCP namespaces receive none. OVS translates the packet to tenant VLAN 1038, but `dms-compute3` physical `eno1` is administratively up without carrier (`link_state: down`). Both MAC and DUID client identifiers fail, so the temporary MAC experiment was reverted and product source remains unchanged. `dms-compute2` and `dms-compute4` tenant uplinks are up. Shared host/switch configuration remains untouched; final verification must identify the healthy placement used.
- Healthy-host diagnostic proof: cold-placement of the same owned VM on `dms-compute2` obtained its original Neutron IPv4 `192.168.0.132` on renamed secondary `ens4`, with provider `ens3`/`117.16.137.222` unchanged, only provider default route, empty secondary DNS and K3s Ready. The DHCP source configuration was unchanged. The earlier live-placement attempt hit a separate Nova/libvirt error (`Target network card MTU 0 does not match source 1500`); no shared Nova, libvirt, host or switch settings were changed. Use cold placement of only temporary verification VMs before first NIC attachment for the final two-node run.
- Final production lifecycle (2026-09-26 11:14–11:26 UTC): a private-network request returned 400 without resource changes, and omitted `network_id` created cluster `1fa8338a-17d9-4e8f-a717-2cb028fb2077` on the administrator external provider. After both nodes were Ready, the owned test VMs were cold-placed before first NIC attachment: server `2e87963e-6f75-45b8-a520-e40d09c5a81a` `dms-compute3`→`dms-compute2`, agent `32e9db8d-4083-4633-b9a0-8fc7b83a0888` `dms-compute13`→`dms-compute4`; node IPs stayed unchanged and Ready. Internal NICs attached through the Afterglow interface API obtained Neutron DHCP addresses `192.168.0.97`/`192.168.0.55` on secondary `ens8`. After hotplug and again after soft reboot, provider `ens3` pins (`117.16.137.225`/`117.16.137.235`), `node-ip`, `flannel-iface`, server `advertise-address`, provider-only default route and `1.1.1.1` egress, empty secondary DNS and `accept_ra=0` held. The downloaded kubeconfig verified TLS without `insecure-skip-tls-verify` against `https://117.16.137.225:6443`; Kubernetes InternalIPs were unchanged and every kube-system pod was Running or Succeeded after reboot. Delete returned 204 and the exact baseline instance, port, floating IP, security group and volume IDs were restored (0/13/0/4/19). Remote and local verifier scripts, kubeconfigs and client keys were removed.
- Pod-route rule gap: the priority-29999 `to 10.42.0.0/16 table main` rule was present on both nodes after hotplug and on the agent after reboot. The server's two proofs 2–7 seconds after its reboot showed `afterglow-k3s-pod-route.service` active but the rule absent. The likely, unproven cause is systemd-networkd dropping foreign rules during the boot-time secondary `netplan apply`, followed by the watcher's 5-second restore loop. The server was not re-sampled before deletion; server Ready and kube-system readiness followed. The verifier did not assert this rule after reboot, so a bounded post-reboot rule assertion remains a verification gap.
