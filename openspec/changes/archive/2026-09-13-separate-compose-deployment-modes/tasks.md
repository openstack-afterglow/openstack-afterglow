## Implementation

- [x] Define source-built local Lumen, Waygate, Drover and Afterglow runtime in docker-compose.dev.yml, preserving existing local data and boundaries.
- [x] Define pull-only production images, mandatory TLS HAProxy/load balancing and catalog-based sibling communication in docker-compose.prod.yml.
- [x] Reduce docker-compose.yml to standalone frontend and backend defaults.
- [x] Update repository deployment rules, commands, examples and affected behavioral contracts; remove obsolete deployment paths.
- [x] Activate the current environment in dev mode without deleting volumes or changing existing keys.
- [x] Verify actual dashboard/OpenStack requests, production ingress behavior and regression gates; record remaining upstream blockers and architecture freshness.

## Runtime evidence

- Resolved base configuration contains only frontend/backend. All 17 production profile services are image-only; only HAProxy publishes ports. Host/TOML dev endpoint poisoning cannot override production catalog routing, and full digest-pinned image references remain unchanged.
- Actual dev source build and startup completed, then all 18 existing containers were recreated against the canonical manifest while preserving mount/volume identities, private config and encryption/JWT/service-key bytes. Four migrations/bootstrap jobs exited 0 and 14 services are running. No volume deletion or remote OpenStack mutation was performed. An unrelated `/blog` container was independently recreated during this task; none of our commands targeted it.
- Standalone consolidation initially omitted Drover's callback URL, causing startup validation to reject the service. Restored required Drover/Waygate callback values and Waygate key-name handling before successful cutover; did not bypass validation.
- Actual HAProxy 3.2 with disposable certificate/origin containers passed certificate-verified HTTPS, HTTP→HTTPS redirect, backend/frontend round-robin across two replicas each, spoofed forwarded-header replacement, Lumen TLS listener routing, early SSE delivery, WebSocket upgrade/frame forwarding and missing-certificate rejection. All temporary ingress containers/network/certificate files were removed. This is configuration/ingress proof, not a production deployment.
- Normal local login returns 200 and the two existing Lumen models remain available. Full CLI smoke passes sibling/BFF/billing/context checks and Drover stats, but exits 1 on Nova-backed dashboard summary/quotas 503. The affected non-admin DMSLAB session's deployed handler still returns `{ "total": 0, "active": 0, "available": true }`. The temporary owned empty context conversation was deleted with HTTP 204; no provider completion occurred.
- A fresh isolated Chrome session logged in through the real frontend and switched to DMSLAB through the normal project selector. The deployed dashboard shows instance/quota warnings without horizontal overflow; browser network entries confirm summary/quotas 503 and trend/Drover stats 200. Direct unauthenticated Nova discovery on controller ports 8774 timed out on controller1/2 and could not connect on controller3. No remote recovery was attempted without controller access.
- Named config/contracts passed (141 configuration tests and 120 contracts). Final `npm run test:gate` passed: 2,721 backend unit, 1,285 frontend, 120 contracts, 24 real-datastore functional tests, orchestration and backend lint/format. Current published backend `latest` registry metadata resolves successfully and contains linux/amd64 plus attestation, not arm64.
- The separate parent change `consolidate-local-compose-runtime` remains active on the unresolved Nova/dashboard acceptance criterion. This Compose-mode change completes its configuration/cutover/verification scope without claiming Nova recovered or a production cloud deployment occurred.
- Architecture stamp and `python3 scripts/check_architecture.py --staged` passed against reviewed source fingerprint `bc2a52a4af7d335aeaa568d80c0117befa00952f39ca41c7ee4159ba87bbb617` (2,002 files). Staged verification used a disposable index; the real index was byte-identical afterward. Final running container image IDs match every `:current` source tag. The isolated browser/profile was removed; the actual dev stack remains running. No commit or push was performed.
