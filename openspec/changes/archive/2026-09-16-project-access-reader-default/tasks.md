## Implementation Tasks

- [x] Replace per-add role picker with immediate `reader` assignment for users and groups.
- [x] Group project members by principal and render role chips.
- [x] Add per-member `세부 권한` checkbox sub-modal with locked `reader`.
- [x] Add `제거` (revoke all roles) per member and hide `+` for existing user members.
- [x] Add component tests and verify responsive rendering.
