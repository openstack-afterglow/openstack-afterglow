# Kolla base Afterglow configuration layer. Applied before the generated
# operator and final Kolla layers; do not edit this generated file.

[openstack]
auth_url = "{{ afterglow_keystone_auth_url }}"
project_name = "{{ afterglow_service_project_name }}"
project_domain_name = "{{ afterglow_keystone_project_domain_name }}"
user_domain_name = "{{ afterglow_keystone_user_domain_name }}"
region_name = "{{ afterglow_keystone_region_name }}"
interface = "{{ afterglow_keystone_interface }}"
insecure = {{ afterglow_openstack_insecure | bool | lower }}
cacert = "{{ afterglow_openstack_cacert }}"
username = "{{ afterglow_admin_keystone_user }}"
password = "{{ afterglow_admin_keystone_password }}"
service_project_id = "{{ afterglow_service_project_id }}"
ceph_monitors = "{{ afterglow_ceph_monitors }}"
manila_share_type = "{{ afterglow_manila_share_type }}"

[app]
backend_port = {{ afterglow_backend_port }}
frontend_port = {{ afterglow_frontend_port }}
secret_key = "{{ afterglow_secret_key }}"
public_api_base = "{{ afterglow_public_api_base }}"
frontend_base_url = "{{ afterglow_public_endpoint_url }}"
site_name = "Afterglow"
site_description = "OpenStack VM + OverlayFS 배포 플랫폼"
logo_path = "/logo.png"
logo_dark_path = "/logo-white.png"
logo_light_path = "/logo-dark.png"
favicon_path = "/favicon.ico"
refresh_interval_ms = 5000

[cache]
redis_url = "{{ afterglow_redis_url }}"
sentinel_enabled = {{ afterglow_sentinel_enabled | bool | lower }}
sentinel_master_name = "{{ afterglow_sentinel_master_name }}"
sentinel_hosts = "{{ afterglow_sentinel_hosts }}"
ttl_fast = 15
ttl_normal = 30
ttl_slow = 60
ttl_static = 300
default_ttl_seconds = 30

[session]
timeout_seconds = 3600
warning_before_seconds = 300
absolute_timeout = 14400

[nova]
default_network_id = ""
default_availability_zone = "nova"
boot_volume_size_gb = 20
upper_volume_size_gb = 50
default_network_enabled = true
default_network_cidr = "192.168.0.0/24"
default_network_external_id = ""

[services]
magnum = {{ afterglow_service_magnum_enabled | bool | lower }}
manila = {{ afterglow_service_manila_enabled | bool | lower }}
zun = {{ afterglow_service_zun_enabled | bool | lower }}
k3s = {{ afterglow_service_k3s_enabled | bool | lower }}
swift = {{ afterglow_service_swift_enabled | bool | lower }}
trove = {{ afterglow_service_trove_enabled | bool | lower }}
barbican = {{ afterglow_service_barbican_enabled | bool | lower }}
waygate = {{ afterglow_service_waygate_enabled | bool | lower }}
palimpsest = {{ afterglow_service_palimpsest_enabled | bool | lower }}
chat = {{ afterglow_service_chat_enabled | bool | lower }}
mcp = {{ afterglow_service_mcp_enabled | bool | lower }}

[instance_health]
callback_base_url = "{{ afterglow_instance_health_callback_base_url }}"

[k3s]
# Shared legacy AES-256-GCM key; Drover owns its own kubeconfig encryption key.
kubeconfig_encryption_key = "{{ afterglow_kubeconfig_encryption_key | default('') }}"
# GPU admission and provisioning credentials are injected through the container environment.
gpu_admission_token = "{{ afterglow_k3s_gpu_admission_token | default('') }}"
# provisioning_token is injected as K3S_PROVISIONING_TOKEN from the secret environment.

[builder]
image_id = ""
flavor_id = ""
network_id = ""

[union]
layer_store_rw_share_id = "{{ afterglow_share_layer_store_rw_id }}"
layer_store_ro_share_id = "{{ afterglow_share_layer_store_ro_id }}"
manifest_store_share_id = "{{ afterglow_share_manifest_store_id }}"

[database]
url = "{{ afterglow_database_url }}"
pool_size = 5
max_overflow = 10
auto_create_tables = false

[cors]
origins = "{{ afterglow_public_endpoint_url }}"

[gitlab_oidc]
enabled = false
gitlab_url = "https://gitlab.com"
client_id = ""
client_secret = "{{ afterglow_oidc_client_secret | default('') }}"
idp_id = "gitlab"
protocol_id = "openid"
redirect_uri = "{{ afterglow_public_endpoint_url }}/auth/gitlab/callback"
scopes = "openid email profile read_user"

[logging]
log_directory = "/var/log/kolla/afterglow_api"
log_level = "INFO"
max_bytes = 52428800
